all:
  children:
    dockerhosts:
      hosts: 
        nuc:
          ansible_user: dan
    accesspoint:
      hosts:
        pi-router:
          ansible_user: pi
    dashboard:
      hosts:
        pi-leo:
          ansible_user: pi
          deploy_directory: /usr/local/src
